import chart from './support-size-info/chart';
import table from './support-size-info/table';

export default {
  chart,
  table,
  total: 'total',
  mode: {
    chart: 'Chart',
    table: 'Table',
  },
  chartDisabledTooltip: 'The chart view is unavailable because the number of spaces exceeds the display limit.',
  noResultsFound: 'No results found',
};
