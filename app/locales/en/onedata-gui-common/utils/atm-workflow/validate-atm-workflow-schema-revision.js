export default {
  laneIteratorStoreMissing: 'Lane "{{laneName}}" has no store to iterate on. Modify lane properties to fix this issue.',
  taskArgumentSourceStoreMissing: 'Task "{{taskName}}" is trying to use a non-existing store as a source for argument "{{argumentName}}" value. Adjust that argument assignments to fix this issue.',
  taskResultTargetStoreMissing: 'Task "{{taskName}}" is trying to use a non-existing store as a target for result "{{resultName}}" value. Adjust that result assignment to fix this issue.',
};
