<div class="item-header">
  <OneIcon @icon={{@icon}} /><span class="one-label">{{@name}}</span>
  {{#if @tip}}
    <OneTooltip @title={{@tip}} @placement="right" @delayShow={{200}} />
  {{/if}}
</div>
