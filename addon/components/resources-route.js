import Component from '@ember/component';
import layout from 'onedata-gui-common/templates/components/resources-route';

export default Component.extend({
  layout,
  classNames: [
    'resources-route'
  ],
});
