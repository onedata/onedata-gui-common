import Component from '@ember/component';
import layout from '../templates/components/one-pseudo-modal';

export default Component.extend({
  layout,
  classNames: ['one-pseudo-modal', 'modal', 'in'],
});
