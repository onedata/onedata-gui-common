import Component from '@ember/component';
import layout from 'onedata-gui-common/templates/components/one-list';

export default Component.extend({
  layout,
  tagName: 'ul',
  classNames: ['one-list'],
});
