<RenderLater @triggerRender={{isOpened}}>
  <OneWebuiPopover
    @popoverStyle="record-info"
    @open={{isOpened}}
    @triggerSelector={{triggerSelector}}
    @placement={{if (media "isMobile") "bottom" "auto"}}
    as |popover|
  >
    <ClickOutside
      @onClickOutside={{action (mut isOpened) false}}
      @exceptSelector={{triggerSelector}}
    >
      {{yield}}
    </ClickOutside>
  </OneWebuiPopover>
</RenderLater>
