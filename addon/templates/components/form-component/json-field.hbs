{{json-editor
  disabled=(not isEffectivelyEnabled)
  readonly=isInViewMode
  value=value
  isErrorMsgVisible=false
  useBootstrapClasses=false
  inputId=fieldId
  onChange=(action "valueChanged")
  onFocusLost=(action "focusLost")}}
{{yield}}
