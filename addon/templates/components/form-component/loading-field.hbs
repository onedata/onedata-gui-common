{{#if isPending}}
  <SpinSpinnerBlock @sizeClass={{spinnerSize}} />
  {{#if loadingText}}
    <span class="loading-text">{{loadingText}}</span>
  {{/if}}
{{else if isRejected}}
  <ResourceLoadError @reason={{loadingError}} />
{{/if}}
{{yield}}
