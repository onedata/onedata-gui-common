{{one-way-radio-group
  class=inputClasses
  inputId=fieldId
  isReadOnly=(not isEnabled)
  value=value
  options=preparedOptions
  update=(action "valueChanged")
  onFocusOut=(action "focusLost")}}
{{yield}}
