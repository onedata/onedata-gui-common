{{#if icon}}
  <OneIcon @icon={{icon}} />
{{/if}}
{{#if sidebarTitle}}
  <span class="button-label">{{sidebarTitle}}</span>
{{/if}}
{{#if tip}}
  <OneTooltip @title={{tip}} @placement="bottom" />
{{/if}}
{{yield}}
