{{#if valueContainer.success}}
  <AtmWorkflow::ValuePresenters::FullValuePresenter
    @dataSpec={{dataSpec}}
    @value={{valueContainer.value}}
    @context={{context}}
  />
{{else}}
  <ResourceLoadError @reason={{valueContainer.error}} />
{{/if}}
