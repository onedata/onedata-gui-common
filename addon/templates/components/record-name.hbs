{{#if record.username}}
  <span class="record-name-general">{{record.name}}</span>
  <span class="record-username">({{record.username}})</span>
{{else}}
  <NameConflict @item={{record}} @tagName="span" class="record-name-general" />
{{/if}}