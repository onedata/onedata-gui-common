<div class="tags-input">
  <span class="stores-label">{{tt this "label"}}:</span>
  {{#each sortedStores as |store|}}
    <WorkflowVisualiser::StoresList::Store
      @store={{store}}
      @mode={{mode}}
      @actionsFactory={{actionsFactory}}
    />
  {{/each}}
  {{#if (eq mode "edit")}}
    <OneButton
      class={{concat-classes createAction.className "input-element"}}
      @onClick={{createAction.executeCallback}}
      @size="sm"
    >
      <OneIcon @icon={{createAction.icon}} />
      <span class="text">{{createAction.title}}</span>
    </OneButton>
  {{/if}}
</div>
{{yield}}
