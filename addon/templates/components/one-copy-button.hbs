{{#copy-button
  class=class
  parentElementId=elementId
  clipboardTarget=clipboardTarget
  success=success
  error=error
}}
  {{#if hasBlock}}
    {{yield}}
  {{else}}
    {{one-icon icon="browser-copy"}} {{tt this "copyBtn"}}
  {{/if}}
{{/copy-button}}
