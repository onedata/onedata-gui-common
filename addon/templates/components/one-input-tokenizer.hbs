<input
  id={{inputId}}
  class="{{inputClass}}"
  disabled={{if internalDisabled "disabled" ""}}
  type="text"
>
{{#if isBusy}}
  <SpinSpinnerBlock
    @sizeClass="xs"
    class="absolute-middle-centered input-busy-spinner"
  />
{{/if}}
