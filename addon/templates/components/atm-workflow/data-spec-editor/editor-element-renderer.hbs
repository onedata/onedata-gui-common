{{#if editorElementComponentName}}
  {{component editorElementComponentName
    mode=mode
    isEnabled=isEnabled
    placementContext=placementContext
    dataTypeFilters=dataTypeFilters
    editorElement=editorElement
    parentEditorElement=parentEditorElement
    editorElementsContextMap=editorElementsContextMap
    onElementChange=onElementChange
    onFocusLost=onFocusLost
  }}
{{/if}}
{{yield}}
