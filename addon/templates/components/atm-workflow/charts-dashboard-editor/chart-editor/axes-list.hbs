<div class="list-header">
  {{#one-button
    class="add-axis-btn"
    type="primary"
    size="sm"
    onClick=(action "addAxis")
  }}
    {{one-icon icon="plus"}}
    <span class="text">{{tt this "addAxis"}}</span>
  {{/one-button}}
</div>

