{{#each actionsToRender as |actionSpec|}}
  <span
    class="action {{actionSpec.name}}-action clickable"
    onClick={{action actionSpec.name}}
  >
    <OneIcon @icon={{actionSpec.icon}} />
    <OneTooltip @title={{tt this actionSpec.name}} />
  </span>
{{/each}}
