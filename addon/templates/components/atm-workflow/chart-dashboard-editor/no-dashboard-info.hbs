<h3 class="header">
  {{tt this "header"}}
  <OneLabelTip @title={{tt this "headerTip"}} />
</h3>
{{#if (not editorContext.isReadOnly)}}
  <p class="description">{{tt this "description"}}</p>
  <OneButton
    class="create-btn"
    @type="primary"
    @onClick={{onCreateDashboard}}
  >
    <OneIcon @icon="plus" />
    <span class="text">{{tt this "createButton"}}</span>
  </OneButton>
{{/if}}
