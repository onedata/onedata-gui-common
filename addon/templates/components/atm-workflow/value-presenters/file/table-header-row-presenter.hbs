{{yield (hash section="start")}}
{{#each (array "name" "path" "size") as |colName|}}
  <th class="column-{{dasherize colName}}">
    <TruncatedString>{{tt this colName}}</TruncatedString>
  </th>
{{/each}}
{{yield (hash section="end")}}
