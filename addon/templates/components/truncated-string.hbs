{{yield}}{{#render-later triggerRender=showTooltip as |renderLater|}}{{one-tooltip
    class=tooltipClass
    autoPlacement=true
    placement=tooltipPlacement
    title=tooltipText
    triggerEvents=""
    visible=showTooltip
    onHidden=renderLater.resetRenderTrigger}}{{/render-later}}