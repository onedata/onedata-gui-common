{{yield}}
{{#render-later triggerRender=showTooltip as |renderLater|}}
  {{one-tooltip
    autoPlacement=true
    placement="top" 
    title=tooltipText
    triggerEvents=""
    visible=showTooltip
    onHidden=renderLater.resetRenderTrigger}}
{{/render-later}}
