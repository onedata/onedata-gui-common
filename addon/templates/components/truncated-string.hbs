{{#if widthBased}}
  <div class="truncate truncated-string-content">
    {{yield}}
  </div>
{{else}}
  {{yield}}
{{/if}}
{{one-tooltip autoPlacement=true placement="top" title=tooltipText triggerEvents="" visible=(and tooltipEnabled showTooltip)}}
