{{#if isLoaded}}
  {{yield}}
{{else}}
  {{#if isLoading}}
    {{spin-spinner-block sizeClass="md" class="middle-centered"}}
  {{else if isError}}
    {{resource-load-error reason=errorReason}}
  {{/if}}
{{/if}}
