import { expect } from 'chai';
import { describe, it } from 'mocha';
import oneHistogramDataFunctionsLoadSeries from 'onedata-gui-common/utils/one-histogram/data-functions/load-series';

describe('Unit | Utility | one histogram/data functions/load series', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = oneHistogramDataFunctionsLoadSeries();
    expect(result).to.be.ok;
  });
});
