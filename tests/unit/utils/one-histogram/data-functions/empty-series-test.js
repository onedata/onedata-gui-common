import { expect } from 'chai';
import { describe, it } from 'mocha';
import oneHistogramDataFunctionsEmptySeries from 'onedata-gui-common/utils/one-histogram/data-functions/empty-series';

describe('Unit | Utility | one histogram/data functions/empty series', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = oneHistogramDataFunctionsEmptySeries();
    expect(result).to.be.ok;
  });
});
