import { expect } from 'chai';
import { describe, it } from 'mocha';
import oneHistogramDataFunctionsMultiply from 'onedata-gui-common/utils/one-histogram/data-functions/multiply';

describe('Unit | Utility | one histogram/data functions/multiply', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = oneHistogramDataFunctionsMultiply();
    expect(result).to.be.ok;
  });
});
