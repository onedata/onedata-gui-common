import { expect } from 'chai';
import { describe, it } from 'mocha';
import oneHistogramDataFunctionsIndex from 'onedata-gui-common/utils/one-histogram/data-functions/index';

describe('Unit | Utility | one histogram/data functions/index', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = oneHistogramDataFunctionsIndex();
    expect(result).to.be.ok;
  });
});
