import { expect } from 'chai';
import { describe, it } from 'mocha';
import oneHistogramSeriesFunctionsAbs from 'onedata-gui-common/utils/one-histogram/series-functions/abs';

describe('Unit | Utility | one histogram/series functions/abs', function () {
  // Replace this with your real tests.
  it('works', function () {
    let result = oneHistogramSeriesFunctionsAbs();
    expect(result).to.be.ok;
  });
});
