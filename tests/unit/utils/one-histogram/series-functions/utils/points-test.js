import { expect } from 'chai';
import { describe, it } from 'mocha';
import oneHistogramSeriesFunctionsUtilsPoints from 'onedata-gui-common/utils/one-histogram/series-functions/utils/points';

describe('Unit | Utility | one histogram/series functions/utils/points', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = oneHistogramSeriesFunctionsUtilsPoints();
    expect(result).to.be.ok;
  });
});
