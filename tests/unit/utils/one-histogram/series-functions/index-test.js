import { expect } from 'chai';
import { describe, it } from 'mocha';
import oneHistogramSeriesFunctionsIndex from 'onedata-gui-common/utils/one-histogram/series-functions/index';

describe('Unit | Utility | one histogram/series functions/index', function () {
  // Replace this with your real tests.
  it('works', function () {
    let result = oneHistogramSeriesFunctionsIndex();
    expect(result).to.be.ok;
  });
});
