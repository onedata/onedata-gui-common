import { expect } from 'chai';
import { describe, it } from 'mocha';
import oneHistogramTransformFunctionsIndex from 'onedata-gui-common/utils/one-histogram/transform-functions/index';

describe('Unit | Utility | one histogram/transform functions/index', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = oneHistogramTransformFunctionsIndex();
    expect(result).to.be.ok;
  });
});
