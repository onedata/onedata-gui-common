import { expect } from 'chai';
import { describe, it } from 'mocha';
import oneHistogramTransformFunctionsMultiply from 'onedata-gui-common/utils/one-histogram/transform-functions/multiply';

describe('Unit | Utility | one histogram/transform functions/multiply', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = oneHistogramTransformFunctionsMultiply();
    expect(result).to.be.ok;
  });
});
