import { expect } from 'chai';
import { describe, it } from 'mocha';
import oneHistogramTransformFunctionsAsBytesPerSecond from 'onedata-gui-common/utils/one-histogram/transform-functions/as-bytes-per-second';

describe('Unit | Utility | one histogram/transform functions/as bytes per second', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = oneHistogramTransformFunctionsAsBytesPerSecond();
    expect(result).to.be.ok;
  });
});
