import { expect } from 'chai';
import { describe, it } from 'mocha';
import oneHistogramTransformFunctionsAbs from 'onedata-gui-common/utils/one-histogram/transform-functions/abs';

describe('Unit | Utility | one histogram/transform functions/abs', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = oneHistogramTransformFunctionsAbs();
    expect(result).to.be.ok;
  });
});
