import { expect } from 'chai';
import { describe, it } from 'mocha';
import oneHistogramTransformFunctionsSupplyValue from 'onedata-gui-common/utils/one-histogram/transform-functions/supply-value';

describe('Unit | Utility | one histogram/transform functions/supply value', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = oneHistogramTransformFunctionsSupplyValue();
    expect(result).to.be.ok;
  });
});
