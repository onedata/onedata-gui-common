import { expect } from 'chai';
import { describe, it } from 'mocha';
import OneHistogramState from 'onedata-gui-common/utils/one-histogram/state';

describe('Unit | Utility | one histogram/state', function () {
  // Replace this with your real tests.
  it('works', function () {
    let result = OneHistogramState();
    expect(result).to.be.ok;
  });
});
