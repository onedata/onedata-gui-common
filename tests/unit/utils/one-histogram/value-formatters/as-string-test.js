import { expect } from 'chai';
import { describe, it } from 'mocha';
import asString from 'onedata-gui-common/utils/one-histogram/value-formatters/as-string';

describe('Unit | Utility | one histogram/value formatters/as string', function () {
  // Replace this with your real tests.
  it('works', function () {
    let result = asString();
    expect(result).to.be.ok;
  });
});
